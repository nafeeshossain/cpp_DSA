#include <iostream>
using namespace std;

int main(){
    int x;
    cin>>x;

    // if(x%2==0) cout<<x<<" is Even";
    // else cout<<x<<" is Odd";

    // Conditions ? True : False

    x%2==0 ? cout<<"Even" : cout<<"Odd";

    return 0;
}
